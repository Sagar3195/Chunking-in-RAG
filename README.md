# Chunking-in-RAG
Chunking in Retrieval-Augmented Generation (RAG) refers to how you split your source documents into smaller pieces (“chunks”) before indexing and retrieval. Good chunking drastically improves retrieval accuracy and reduces hallucinations.
